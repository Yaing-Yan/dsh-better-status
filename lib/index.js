/**
 * dsh-better-status — Host half.
 *
 * Minimal no-op: the package must be a Loader entry for the browser half to be
 * discovered and served. All rendering happens in the client bundle.
 */
export const name = 'dsh-better-status'

export const inject = []

export function apply(ctx) {}
